[project]
name = "computer-use"
version = "0.1.0"
description = "OpenAI computer-use-agent example"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai",
    "pillow>=12.1.1",
    "playwright",
    "pyautogui>=0.9.54",
    "python-dotenv",
]
