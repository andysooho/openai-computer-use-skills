[project]
name = "computer-use"
version = "0.1.0"
description = "OpenAI computer-use-agent example"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai",
    "playwright",
    "python-dotenv",
]
